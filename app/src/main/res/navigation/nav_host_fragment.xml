<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_host_fragment"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.archi.cosplay_planner.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/newCosplayActivity"
        android:name="com.archi.cosplay_planner.NewCosplayActivity"
        android:label="NewCosplayActivity" />
    <action
        android:id="@+id/to_new"
        app:destination="@id/newCosplayActivity" />
</navigation>